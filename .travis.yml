dist: trusty
sudo: false
language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            # install clang and ninja
            - ninja-build
            - clang
            
script:
    - cmake -G Ninja .
    - ninja
    - ctest
